-- Script configuration
local config = {
    -- Nametag ([Connect Message] The User ... has disconnected)
    -- Change the value of nameTag as needed
    nameTag = "Connect Message",

    -- Show bot notifications. true = yes, false = no
    -- Change the value of botNotification to true or false as needed
    botNotification = true,

    -- Select the language (Need to be in cmlanguages.lua)
    -- Change the value of language to the desired language code (e.g., "es" for Spanish)
    language = "en",
}

-- Return the configuration
return config
